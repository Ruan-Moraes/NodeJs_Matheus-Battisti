<h1>Livros</h1>
<div class='books-container'>
  {{#each books}}
    <div class='book'>
      <span>ID - #{{id}}</span>
      <a href='/books/{{id}}'>
        <h2>{{title}}</h2>
      </a>
      <p>{{pageqty}} páginas</p>
    </div>
  {{/each}}
</div>